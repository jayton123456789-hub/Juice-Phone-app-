{
  "name": "juice-phone-app",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "author": "Jayton",
  "description": "Juice WRLD Music Player Desktop App",
  "main": "dist-electron/main.cjs",
  "scripts": {
    "dev": "npm run build:electron && electron .",
    "build": "tsc && vite build",
    "build:electron": "tsc -p electron/tsconfig.json && npm run rename:electron",
    "dist": "npm run build && npm run build:electron && electron-builder",
    "dist:win": "npm run build && npm run build:electron && electron-builder --win",
    "preview": "vite preview",
    "rename:electron": "node rename-electron.cjs"
  },
  "dependencies": {
    "axios": "^1.6.7",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-icons": "^5.5.0",
    "react-router-dom": "^6.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.16",
    "@types/react": "^18.2.55",
    "@types/react-dom": "^18.2.19",
    "@vitejs/plugin-react": "^4.2.1",
    "electron": "^28.2.0",
    "electron-builder": "^24.9.1",
    "typescript": "^5.3.3",
    "vite": "^5.1.0"
  },
  "build": {
    "appId": "com.juice.phoneapp",
    "productName": "Juice Phone",
    "directories": {
      "output": "release"
    },
    "files": [
      "dist",
      "dist-electron"
    ],
    "win": {
      "target": "portable",
      "signAndEditExecutable": false
    },
    "portable": {
      "artifactName": "Juice Phone.exe"
    }
  }
}
